<p id="notice"><%= notice %></p>

<p>
  <strong>Question:</strong>
  <%= @question.question %>
</p>

<p>
  <strong>Topic:</strong>
  <%= @question.topic&.name%>
</p>

<h3>Comments</h3>
<%= render partial: 'questions/answer', collection: @question.answers.reverse %>

<%= render partial: 'answers/form' %>


<%= link_to 'Back', users_path %>
